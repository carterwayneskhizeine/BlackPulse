version: '3.8'

services:
  message-board:
    build: .
    container_name: message-board-app
    ports:
      - "1989:1989"
    volumes:
      # Mount the local data directory to the container's data directory for persistence
      - ./data:/app/data
    restart: unless-stopped
