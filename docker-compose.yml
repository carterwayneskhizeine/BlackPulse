services:
  message-board:
    build: .
    container_name: black-pulse
    ports:
      - "0.0.0.0:1989:1989"
    env_file:
      - ./.env
    volumes:
      # Mount the local data directory to the container's data directory for persistence
      - ./data:/app/data
    restart: unless-stopped
