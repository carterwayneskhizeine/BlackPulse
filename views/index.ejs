<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Anonymous Message Board</title>
    <link href="/style.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/showdown/dist/showdown.min.js"></script>
</head>
<body class="bg-gray-900 text-gray-200 flex items-center justify-center min-h-screen">

    <div class="container mx-auto p-4 md:p-8 max-w-2xl w-full">

        <!-- Header -->
        <header class="text-center mb-8">
            <h1 class="text-4xl font-bold text-teal-400">Anonymous Message Board</h1>
            <p class="text-gray-400">Post your thoughts without a trace.</p>
        </header>

        <!-- Message Form -->
        <div class="mb-8">
            <form id="message-form" class="flex flex-col gap-4">
                <textarea 
                    id="message-input"
                    name="content" 
                    class="w-full p-4 bg-gray-800 border border-gray-700 rounded-lg focus:ring-2 focus:ring-teal-500 focus:outline-none transition-shadow" 
                    rows="3" 
                    placeholder="What's on your mind?"></textarea>
                <button 
                    type="submit"
                    class="bg-teal-600 hover:bg-teal-700 text-white font-bold py-2 px-4 rounded-lg transition-colors w-full md:w-auto self-end">
                    Post Message
                </button>
            </form>
        </div>

        <!-- Message List -->
        <main id="message-list" class="space-y-4">
            <!-- Messages will be dynamically injected here by JavaScript -->
        </main>

        <!-- Footer -->
        <footer class="text-center mt-12 text-gray-500">
            <p>&copy; 2025 GoldieWeb. All rights reserved.</p>
        </footer>

    </div>

    <script src="/js/main.js"></script>
</body>
</html>
